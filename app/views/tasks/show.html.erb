<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<p><%= @task.completed %></p>
<%= link_to "delete #{@task.title}",
            task_path(@task),
            method: :destroy,
            data: { confirm: "Are you sure?" } %>